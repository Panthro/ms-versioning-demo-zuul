server:
  port: 8081
spring:
  application:
    name: ms-versioning-v1
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    instance-id: ${spring.application.name}
management:
  security:
    enabled: false